services:
  spoolstack:
    container_name: spoolstack
    build: .
    ports:
      - "3000:3000"
    volumes:
      - spoolstack-data:/data
    environment:
      - DATABASE_URL=/data/sqlite.db
    restart: unless-stopped

volumes:
  spoolstack-data:
